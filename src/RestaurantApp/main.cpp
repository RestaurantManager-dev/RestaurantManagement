#include "DataStructures/HashMap.hpp"
#include <iostream>
#include "RestaurantApp/Resturant.h"

int main()
{
    Resturant restaurant;
    /*restaurant.excuteTimestep();
    
    std::cout << "\n=== HashMap Tests ===" << std::endl;
    HashMap<int> hashMap(10);

    // Test add
    std::cout << "Adding key-value pairs..." << std::endl;
    hashMap.add(1, 100);
    hashMap.add(5, 200);
    hashMap.add(11, 300); // Should hash to same bucket as 1
    hashMap.add(15, 400);
    hashMap.add(25, 500);

    // Test get
    std::cout << "Getting values:" << std::endl;
    std::cout << "Key 1: " << hashMap.get(1) << " (expected: 100)" << std::endl;
    std::cout << "Key 5: " << hashMap.get(5) << " (expected: 200)" << std::endl;
    std::cout << "Key 11: " << hashMap.get(11) << " (expected: 300)"
              << std::endl;
    std::cout << "Key 15: " << hashMap.get(15) << " (expected: 400)"
              << std::endl;
    std::cout << "Key 25: " << hashMap.get(25) << " (expected: 500)"
              << std::endl;
    std::cout << "Key 99: " << hashMap.get(99)
              << " (expected: 0, key doesn't exist)" << std::endl;

    // Test update
    std::cout << "\nUpdating key 5 to 999..." << std::endl;
    hashMap.add(5, 999);
    std::cout << "Key 5: " << hashMap.get(5) << " (expected: 999)" << std::endl;

    // Test remove
    std::cout << "\nRemoving key 11..." << std::endl;
    hashMap.remove(11);
    std::cout << "Key 11: " << hashMap.get(11)
              << " (expected: 0, after removal)" << std::endl;
    std::cout << "Key 1: " << hashMap.get(1)
              << " (expected: 100, should still exist)" << std::endl;

    // Test with strings
    std::cout << "\n=== HashMap<string> Test ===" << std::endl;
    HashMap<std::string> stringMap(5);
    stringMap.add(10, "Hello");
    stringMap.add(20, "World");
    stringMap.add(15, "HashMap");

    std::cout << "Key 10: " << stringMap.get(10) << std::endl;
    std::cout << "Key 20: " << stringMap.get(20) << std::endl;
    std::cout << "Key 15: " << stringMap.get(15) << std::endl;

    std::cout << "\nAll tests completed!" << std::endl;
    */
    return 0;
}